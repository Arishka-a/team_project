# Инструкция для команды

## Для всех участников

### Как начать работу

1. **Клонируйте репозиторий** (если еще не сделали):
   ```bash
   git clone <repository-url>
   cd calculator
   ```

2. **Переключитесь на ветку проекта**:
   ```bash
   git checkout epic/fullstack-calculator
   git pull origin epic/fullstack-calculator
   ```

3. **Работайте в своей папке** (см. ниже)

4. **Коммитьте изменения**:
   ```bash
   git add <ваша-папка>
   git commit -m "описание изменений"
   git push origin epic/fullstack-calculator
   ```

---

## Участник 2 - Экран "Настройки"

### Ваша задача
Реализовать экран настроек калькулятора.

### Где работать
📁 `src/main/resources/static/screen2/`

### Что нужно создать

1. **index.html** - HTML страница настроек
2. **style.css** - Стили для вашего экрана
3. **app.js** - JavaScript логика
4. **locales/ru.json** - Русские переводы
5. **locales/en.json** - Английские переводы

### Функциональность экрана

#### Компоненты:
- Выбор языка (выпадающий список: RU, EN, DE)
- Переключатель темы (светлая/темная)
- Слайдер точности вычислений (0-10 знаков после запятой)
- Кнопка "Сохранить настройки"
- Кнопка "Сбросить по умолчанию"

#### Данные:
- **Из API**: Сохраненные настройки пользователя (если API будет реализован)
- **Локально**: Тема интерфейса, временные изменения

### API Endpoints (если будете использовать)

Можете создать mock данные или попросить Участника 1 добавить эти endpoints:

```javascript
// Получить настройки
GET /api/settings

Response:
{
  "language": "ru",
  "theme": "light",
  "precision": 2
}

// Сохранить настройки
PUT /api/settings

Request:
{
  "language": "ru",
  "theme": "dark",
  "precision": 5
}
```

### Пример структуры

```
screen2/
├── index.html
├── style.css
├── app.js
└── locales/
    ├── ru.json
    └── en.json
```

### Как коммитить

```bash
git add src/main/resources/static/screen2/
git commit -m "feat: implement settings screen (screen 2)"
git push origin epic/fullstack-calculator
```

### Подсказки

1. **Используйте localStorage** для хранения настроек локально
2. **Посмотрите на экран 1** (`screen1/`) для примера структуры
3. **Переключение темы**: добавьте класс `dark-theme` к body
4. **Мультиязычность**: используйте тот же подход, что в screen1

---

## Участник 3 - Экран "Статистика"

### Ваша задача
Реализовать экран статистики использования калькулятора.

### Где работать
📁 `src/main/resources/static/screen3/`

### Что нужно создать

1. **index.html** - HTML страница статистики
2. **style.css** - Стили для вашего экрана
3. **app.js** - JavaScript логика
4. **locales/ru.json** - Русские переводы
5. **locales/en.json** - Английские переводы

### Функциональность экрана

#### Компоненты:
- График использования операций (можно использовать Chart.js или нарисовать SVG)
- Список "Топ-5 операций"
- Счетчик общего количества вычислений
- Средний результат всех операций
- Кнопка "Обновить статистику"

#### Данные:
- **Из API**: Вся статистика берется с сервера
- **Локально**: Нет

### API Endpoints

Используйте существующий endpoint для получения истории и вычислите статистику на клиенте:

```javascript
// Получить историю
GET /api/history

Response:
[
  {
    "id": 1,
    "operation": "add",
    "operands": [5, 3],
    "result": 8.0,
    "timestamp": 1703001234567
  },
  ...
]
```

Из истории можно вычислить:
- Частоту каждой операции
- Топ-5 операций
- Общее количество
- Средний результат

### Пример структуры

```
screen3/
├── index.html
├── style.css
├── app.js
└── locales/
    ├── ru.json
    └── en.json
```

### Как коммитить

```bash
git add src/main/resources/static/screen3/
git commit -m "feat: implement statistics screen (screen 3)"
git push origin epic/fullstack-calculator
```

### Подсказки

1. **Для графиков**: можете использовать Chart.js (подключить через CDN) или нарисовать простые bar charts с помощью div элементов
2. **Обработка данных**: создайте функцию для анализа истории
3. **Посмотрите на экран 1** (`screen1/`) для примера работы с API
4. **Обновление**: добавьте кнопку "Обновить", которая перезагружает данные из API

---

## Общие рекомендации

### Стиль кода
- Используйте понятные названия переменных
- Добавляйте комментарии к сложной логике
- Форматируйте код (отступы 2 или 4 пробела)

### Тестирование
1. Запустите сервер: `./gradlew bootRun`
2. Откройте ваш экран: `http://localhost:8080/screen2/` или `http://localhost:8080/screen3/`
3. Проверьте все функции
4. Протестируйте на разных языках

### Дизайн
- **Не обязательно делать красивый дизайн**, главное - функциональность
- Можете взять за основу стили из `screen1/style.css`
- Используйте простые компоненты (кнопки, списки, формы)

### Помощь
- Если нужны дополнительные API endpoints - попросите Участника 1
- Если застряли - посмотрите код `screen1/` как пример
- Swagger документация: `http://localhost:8080/swagger-ui.html`

---

## Проверка перед коммитом

- [ ] Экран открывается без ошибок
- [ ] Все компоненты видны на странице
- [ ] Переключение языка работает
- [ ] API запросы выполняются (если используете)
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Файлы созданы в правильной папке

---

## Полезные ссылки

- Главная страница: http://localhost:8080/
- Swagger UI: http://localhost:8080/swagger-ui.html
- Архитектура проекта: `docs/ARCHITECTURE.md`

Удачи! 🚀
